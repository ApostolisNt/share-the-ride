// import type { GeneralConfig } from "@schemas/general";

const defaultLang = "gr";
const general = {
  languages: ["gr", "en"],
  defaultLanguage: defaultLang,
  baseUrl: "https://share-the-ride.gr",
  redirects: [],
};

export default general;
